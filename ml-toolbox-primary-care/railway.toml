[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m uvicorn src.ml_toolbox.serving.api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
